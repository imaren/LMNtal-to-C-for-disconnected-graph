 c(!Y), a(!X).

r1@@ a(!L) :- uniq(!L) | b(!L).
% r2@@ b(!L1), c(!L2) :- a(!L1), !L1 >< !L2.
% r2@@ b(!L1), c(!L2) :- a(!L1), !L2 >< !L1.

% !L1.append(!L2).

% a(c). a(c).
% a(N) :- uniq(N) | b(N).


%  d(!Y), d(!Y), d(!Y).

 //できるし、id管理でもできる例
%  r1@@ a(!L) :- uniq(!L) | b(!L).
%  r2@@ b(!L1),b(!L2) :- a(!L1), !L1 >< !L2.